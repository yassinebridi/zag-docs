# Usage with TypeScript

## Setup and Element refs

TO DO. The idea is to tell users to use `useSetup<HTMLDivElement>(...)`

## API and JSX attributes

TO DO. The idea is to tell users to do `connect<SolidPropTypes>(...)`
