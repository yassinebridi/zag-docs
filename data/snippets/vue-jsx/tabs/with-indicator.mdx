```jsx {10}
// ...
return (
  <div ref={ref} {...api.rootProps}>
    <div {...api.tablistProps}>
      {data.map((item) => (
        <button {...api.getTabProps({ value: item.value })} key={item.value}>
          {item.label}
        </button>
      ))}
      <div {...api.tabIndicatorProps} />
    </div>
    {data.map((item) => (
      <div {...api.getTabPanelProps({ value: item.value })} key={item.value}>
        <p>{item.content}</p>
      </div>
    ))}
  </div>
)
```
